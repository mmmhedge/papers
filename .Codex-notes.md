### 2026-07-04 — Local sync pipeline compatibility
**Problem:** Local auto-sync/build could fail on this Mac because `python3` is Python 3.8, while scripts used Python 3.9+ evaluated type hints; Obsidian auto-pull was only documented, not implemented by the repo.
**Solution:** Added postponed annotations to scripts, made `scripts/auto_pull.sh` a tracked pull/rebuild helper with rebase autostash and a stable Python PATH, and added a launchd job template for 15-minute local pulls.
**Verified:** yes — `python3 scripts/build_web.py` runs locally after the fix.

### 2026-07-04 — Show newly added papers first
**Problem:** The website's newest view used arXiv `published` dates, so papers added on July 4 looked like July 1/2 entries; the Obsidian dashboard note was also included as a paper.
**Solution:** Skipped non-paper notes in `build_web.py` and changed the web UI to sort/display by `date_added` with arXiv date as secondary metadata.
**Verified:** yes — rebuilt `web/papers.json` locally and confirmed July 4 entries sort first.

### 2026-07-04 — Deduplicate arXiv revisions
**Problem:** arXiv revisions could be treated as separate papers, and `2606.21784` existed as both v1 and v2.
**Solution:** Added canonical versionless arXiv ID helpers, dedupe checks in fetch/summarize, and kept only the newer v2 note.
**Verified:** yes — duplicate audit reports zero canonical arXiv duplicates.

### 2026-07-05 — Add RSS blog fetch pipeline
**Problem:** The tracker only fetched arXiv papers, so AI lab blogs and newsletters could not enter the Obsidian/web/Telegram flow.
**Solution:** Added RSS feed fetching, blog-specific summarization/frontmatter, source-aware web UI filters/badges, workflow steps, and multi-source Telegram digest inputs.
**Verified:** yes — RSS fetch returns valid post dicts; compile, web build, synthetic blog note, and multi-file digest loader tests pass.

### 2026-07-06 — Catch up local sync after Mac sleep
**Problem:** The local Obsidian sync LaunchAgent used only `StartInterval`, which launchd can miss while the Mac is asleep.
**Solution:** Added quarter-hour `StartCalendarInterval` triggers, which launchd coalesces and runs once when the Mac wakes.
**Verified:** yes — plist validates and installed LaunchAgent reloads with calendar intervals.

### 2026-07-06 — Telegram digest observability
**Problem:** Today's workflow looked green, but Telegram delivery was hard to verify and API-level failures would not fail the job.
**Solution:** Made Telegram missing secrets fail in GitHub Actions, surfaced HTTP/API errors, escaped link URLs, and logged Telegram message IDs on success.
**Verified:** yes — checked today's Actions log and ran no-network local skip, Actions missing-secret failure, py_compile, and synthetic send tests.

### 2026-07-07 — Summarizer failure visibility
**Problem:** The daily workflow fetched 35 papers, but Claude calls failed for low Anthropic credit and the job still ended green with no new notes.
**Solution:** Made summarization failures raise nonzero and stopped retrying non-retryable 4xx Anthropic errors.
**Verified:** yes — checked today's Actions log, live/local counts, py_compile, empty input, and synthetic summarize failure.

### 2026-07-13 — Daily digest overview restored
**Problem:** The Telegram digest overview existed in code but GitHub Actions did not pass `ANTHROPIC_API_KEY`, so run summaries were skipped.
**Solution:** Passed the Claude key to the Telegram step and sharpened the overview prompt for trends plus background context.
**Verified:** yes — py_compile, no-key digest fallback, and workflow env check.

### 2026-07-13 — Lighter Telegram one-liners
**Problem:** Some digest item blurbs were too dense because Telegram reused full note takeaways verbatim.
**Solution:** Prompted future takeaways to be short/plain and added Telegram-side blurb cleanup plus length caps.
**Verified:** yes — py_compile and synthetic dense-blurb formatting check.

### 2026-07-22 — Deep technical Telegram summaries
**Problem:** Needed a reusable way to create GPT-3/VAE-style paper summaries with real equations, tables, details, and Telegram delivery.
**Solution:** Added `scripts/deep_paper_summary.py` with PDF extraction, Claude prompting, Markdown output, and optional Telegram sending.
**Verified:** yes — py_compile, pypdf import, and CLI help checks pass.

### 2026-07-22 — Hosted deep summary button
**Problem:** Deep paper summaries needed to be launched from the hosted website rather than from a local manual script.
**Solution:** Added a GitHub Actions workflow for secure Claude/Telegram execution and a per-paper website button that dispatches it.
**Verified:** yes — workflow YAML parse, web build, and inline JavaScript syntax checks pass.

### 2026-07-22 — Tighten trusted source badges
**Problem:** arXiv papers could be marked trusted when generated notes or abstracts merely mentioned labs like OpenAI or Anthropic.
**Solution:** Stopped trust inference for arXiv papers, kept trusted badges only for known blog/feed sources, and backfilled arXiv note frontmatter.
**Verified:** yes — web rebuild shows 27 trusted items, 0 non-blog trusted items, and 0 arXiv notes with `trusted: true`.

### 2026-07-22 — Remove website GitHub token prompt
**Problem:** The hosted deep-summary button asked for a GitHub token in the static site, which was a poor and unnecessary UX.
**Solution:** Removed the token/API dispatch path and changed the button to copy workflow inputs and open GitHub's authenticated workflow page.
**Verified:** yes — no token references remain, inline JavaScript syntax passes, and web build still outputs 628 papers.
